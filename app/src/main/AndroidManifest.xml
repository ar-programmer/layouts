<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="android.and04.layouts">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LinearActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name=".TableActivity"
            android:label="@string/table_activity"/>
        <activity android:name=".RelativeActivity"
            android:label="@string/relative_activity"/>
        <activity android:name=".FrameActivity"
            android:label="@string/frame_activity"/>
        <activity android:name=".GridActivity"
            android:label="@string/grid_activity"/>
    </application>

</manifest>